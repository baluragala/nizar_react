/*
describe 
it
beforeEach
afterEach
beforeAll
afterAll
expect
jest

*/

describe("Calculator", () => {
  it("should add two numbers", () => {
    expect(2 + 2).toBe(4);
  });
  it("should multiply two numbers", () => {
    expect(2 * 2).toBe(4);
  });
});
